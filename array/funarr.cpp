#include<bits/stdc++.h>
using namespace std;
void printarr(int arr[],int size)
{
    for (int  i = 0; i < size; i++)
    {
       // cin>>arr[i];
       //break;
       cout<<" "<<arr[i];
    }
    
}

int main()
{
    int size;
    cin>>size;

    int one[size];
    //int n;

    printarr(one,size);
}